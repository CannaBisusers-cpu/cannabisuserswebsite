{
  "name": "cannabisuserswebsite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:integration": "jest --runInBand test/integration --detectOpenHandles",
    "scan-secrets": "node scripts/scan-secrets.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "build": "node scripts/generate-sitemap.js",
    "prepare": "husky install"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "glob": "^8.1.0",
    "husky": "^8.0.0",
    "ioredis-mock": "^8.13.1",
    "jest": "^29.0.0",
    "netlify": "^23.13.0"
  }
}
